config:
  target: "http://127.0.0.1:3000"   # Replace with the URL of your application
  phases:
    - duration: 60                   # Duration of the load test in seconds
      arrivalRate: 2                # Number of virtual users to simulate per second

scenarios:
  - flow:
      - get:
          url: "/"

      - think: 2                      # Wait for 2 seconds before adding tasks

      - flow:
          - post:
              url: "/tasks"
              json:
                task: "{{ random('Task 1', 'Task 2', 'Task 3') }}"   # Randomly select a task name
              expect:
                - statusCode: 200      # Expect HTTP status code 200 for successful task addition
          - post:
              url: "/tasks"
              json:
                task: "{{ random('Task 1', 'Task 2', 'Task 3') }}"   # Randomly select a task name
              expect:
                - statusCode: 200      # Expect HTTP status code 200 for successful task addition
          - post:
              url: "/tasks"
              json:
                task: "{{ random('Task 1', 'Task 2', 'Task 3') }}"   # Randomly select a task name
              expect:
                - statusCode: 200      # Expect HTTP status code 200 for successful task addition
          - post:
              url: "/tasks"
              json:
                task: "{{ random('Task 1', 'Task 2', 'Task 3') }}"   # Randomly select a task name
              expect:
                - statusCode: 200      # Expect HTTP status code 200 for successful task addition
          - post:
              url: "/tasks"
              json:
                task: "{{ random('Task 1', 'Task 2', 'Task 3') }}"   # Randomly select a task name
              expect:
                - statusCode: 200      # Expect HTTP status code 200 for successful task addition

      - think: 2                      # Wait for 2 seconds before deleting tasks

      - flow:
          - delete:
              url: "/tasks/{{ random(0, 4) }}"   # Randomly select a task index to delete
              expect:
                - statusCode: 200      # Expect HTTP status code 200 for successful task deletion
          - delete:
              url: "/tasks/{{ random(0, 4) }}"   # Randomly select a task index to delete
              expect:
                - statusCode: 200      # Expect HTTP status code 200 for successful task deletion
          - delete:
              url: "/tasks/{{ random(0, 4) }}"   # Randomly select a task index to delete
              expect:
                - statusCode: 200      # Expect HTTP status code 200 for successful task deletion
          - delete:
              url: "/tasks/{{ random(0, 4) }}"   # Randomly select a task index to delete
              expect:
                - statusCode: 200      # Expect HTTP status code 200 for successful task deletion
          - delete:
              url: "/tasks/{{ random(0, 4) }}"   # Randomly select a task index to